{
  "name": "@theoutfit/lazyload",
  "version": "1.0.0",
  "description": "This fills a few gaps in image lazy-loading support; it covers the various ways you can use <img> elements, <picture> elements, and CSS background images … but it gets out of the way if the environment natively supports lazy-loading.",
  "main": "dist/lazyload.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && rollup --config"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fromtheoutfit/lazyload.git"
  },
  "keywords": [
    "lazy",
    "lazyload",
    "lazy load",
    "lazy-load",
    "lazy images",
    "lazy-images",
    "lazy background images",
    "lazy css images"
  ],
  "author": "The Outfit, Inc.",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fromtheoutfit/lazyload/issues"
  },
  "homepage": "https://github.com/fromtheoutfit/lazyload/#readme",
  "devDependencies": {
    "rollup": "^2.28.2",
    "rollup-plugin-terser": "^7.0.2"
  },
  "files": [
    "dist"
  ]
}
